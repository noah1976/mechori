# MECHORI Nightly Report

## 作業日

- 2026-07-16

## 作業ブランチ

- `codex/local-prototype`

## 今回の目的

Garage Journalを、本文末尾へのメディア添付ではなく、故障や路上停止も読み物として楽しく残せるnote型編集へ変更しました。同時に、症状検索を投稿一覧ではなく、複数投稿・事例の根拠付き要約を主表示する体験へ寄せました。

## 実施したこと

### Journal編集

- 段落、見出し、引用、写真、動画を順序付きブロックとして保持します。
- 各ブロックの間から文章・見出し・引用・写真・動画を挿入できます。
- ブロックの上下移動、削除、メディア説明を操作できます。
- AIは本文を生成・書換えしません。
- Journalを書かず「整備記録だけ残す」導線を同じ画面に用意しました。
- 旧Journal本文は試作スキーマv5への移行時に段落ブロックへ変換します。

### ホーム

- 故障や路上停止も愛車の物語として読まれることを主題にしました。
- 「今日のことを書く」「整備記録だけ残す」「みんなの経験から調べる」を同じ第一画面へ配置しました。
- フォロー中のJournalを画像付きで主表示し、最近の整備記録は別領域へ維持しました。

### ナレッジ検索

- DEMO事例だけから、確認箇所、原因候補、対応、交換部品、結果を自然文と項目別表示へ整理します。
- 要約の下に、車両一致範囲と確認状態を添えた出典投稿リンクを表示します。
- 出典にない内容を追加しない共通契約と根拠検証テストを追加しました。
- 検索条件が空の間は回答を生成せず、情報不足状態を表示します。
- 自分の非公開整備記録は要約と分離して表示します。

## 検証結果

- lint: 成功
- TypeScript型チェック: 成功
- 単体テスト: 45件成功
- production build: 成功
- 390 x 844: Journal、ホーム、検索で横方向のはみ出しなし
- 1440 x 900: ホーム、Journal詳細、検索で横方向のはみ出しなし
- Journal詳細で文章、画像、文章のブロック順序を確認
- 検索条件「警告」で2件のDEMO事例、要約、2件の出典リンクを確認

## ディレクション判断

- 投稿の楽しさは装飾ではなく、知識が集まるための前提条件として扱います。
- ブログ本文を書く利用者と、整備記録だけを残す利用者を上下関係にしません。
- AI要約を回答の主表示としますが、投稿へのリンクを検証可能な根拠として必ず残します。
- 人気、フォロワー、リアクションは検索根拠の強さや確認状態に使用しません。

## 外部接続・費用

- Supabase、Vercel、OpenAI API、OCR Providerへ接続していません。
- 有料サービス、決済、広告SDK、アクセス解析を追加していません。
- 本番デプロイ、mainへの反映を行っていません。

## 所有者に確認してほしいこと

1. `/journal/new`のブロック挿入・移動は、書くことを楽しめそうな操作感か。
2. `/`のトップは「読んでみたい・書いてみたい」と「整備記録だけ」のバランスが適切か。
3. `/search?q=警告`の要約を先に読み、必要時に出典へ遡る順序がイメージに合うか。

## 次回推奨作業

1. 操作確認を受け、ブロック操作、フィード密度、検索要約の文体を調整する。
2. 実Journalデータを用意できた段階で、抽出候補と要約の根拠追跡を評価する。
3. 実画像・動画を用意できた段階で、公開用メディア処理とナンバー候補検出を評価する。
