# DECISIONS

## 決定記録

この文書は、プロジェクト方針・技術方針・法務/安全/プライバシーに関する判断を記録します。未確定事項は断定せず、`docs/BACKLOG.md` の `NEEDS_OWNER` にも反映します。

## 2026-07-12

### 決定: 初期タスクは文書作成のみ（完了）

- 背景: 今後の Codex 作業の基準を先に作るため。
- 決定: アプリケーションコード、データベース、外部サービス接続、依存パッケージ導入は行わない。
- 影響: この時点では実行可能なアプリは存在しない。

### 決定: ローカルWebプロトタイプを作成する

- 背景: 文書だけでなく、翌朝に製品方向と主要操作を確認できる状態へ進めるため。
- 決定: Next.js、TypeScript、npm workspacesで、ローカル完結のWebプロトタイプを作る。
- 影響: アプリケーションコードと必要最小限の依存を追加する。外部サービス、本番環境、認証、課金には接続しない。

### 決定: localStorageを暫定DataProviderにする

- 背景: Supabase導入前に、登録・編集・再読み込み後の保持を追加費用なしで検証するため。
- 決定: 画面はDataProvider境界を利用し、localStorage実装を暫定採用する。
- 影響: データはブラウザ内に限定される。将来Supabase Providerへ差し替える際も、ドメイン型と画面フローを維持しやすくする。

### 決定: DEMOデータと生成画像を実在情報から分離する

- 背景: FIAT Barchettaを題材にしつつ、架空の整備情報を事実として見せないため。
- 決定: 車両画像と少数の整備記録を `DEMO` と明示し、実在部品番号、工場名、具体的作業手順を含めない。
- 影響: プロトタイプは画面・状態確認に利用し、整備判断の情報源には利用しない。

## 2026-07-13

### 決定: 1回の整備機会と個別作業を分離する

- 背景: 車検や入庫の明細には複数の点検・交換作業が含まれるため。
- 決定: 1回の入庫・整備機会を`MaintenanceEvent`とし、複数の`MaintenanceAction`を配下に持つ。症状・警告・点検指摘は`Observation`、部品は作業に紐付く`PartUsage`として扱う。
- 影響: 現行プロトタイプの単一`MaintenanceRecord`型は次回実装で段階的に置き換える。

### 決定: 取込下書き、個人記録、公開ナレッジを分離する

- 背景: OCR・AI候補を事実として確定せず、個人記録の削除と公開ナレッジの訂正を別々に扱うため。
- 決定: `ImportSession`、`ExtractedCandidate`、`FieldAssertion`を経てユーザーが確認し、非公開個人記録へ確定する。共有時は別の共有候補と公開ナレッジを生成する。
- 影響: 取込完了や個人記録の保存だけで、情報が公開候補または公開状態へ移ることはない。

### 決定: ホームは検索と取込の作業起点にする

- 背景: 現行モックは一般的なダッシュボードに見え、MECHORI固有の価値が伝わりにくかったため。
- 決定: 次回UIでは大きなブランド説明を主役にせず、登録車両を範囲とした症状検索、記録取込、未確認下書きを第一画面へ置く。
- 影響: 現行デザインは構造確認用として保持し、実物資料の取込体験と合わせて再設計する。

### 決定: 初期実証車種は FIAT Barchetta

- 背景: 具体的な事例でMVPを検証するため。
- 決定: 初期実証車種は FIAT Barchetta とする。
- 影響: 内部構造は他メーカー、他車種、バイク等へ拡張可能にする。

### 決定: MECHORI は故障診断サービスではない

- 背景: 整備情報は安全上のリスクが高い。
- 決定: 故障診断、修理指示、整備書または専門家の代替を提供しない。
- 影響: 文言、データ構造、AI方針、UI警告で断定を避ける。

### 決定: Privacy by Design を基本原則にする

- 背景: 各国の個人情報保護法制への対応負担を最小化するため。
- 決定: 不要な個人情報を取得・保存しない。
- 影響: VIN全文、ナンバー、正確な位置情報、原本画像等は標準で保持しない。

### 決定: 初期MVPに広告SDKと決済を入れない

- 背景: 固定費・初期費用を抑え、プライバシー影響を減らすため。
- 決定: 初期MVPには広告SDK、Stripe決済、有料相談、部品販売を入れない。
- 影響: 収益化は将来設計に留める。

### 決定: MECHORIの北極星を旧車趣味の参入・維持障壁の低減に置く

- 背景: 整備ログの保存自体ではなく、分散した経験を知識基盤に変え、オーナーと専門外の工場が既存事例を安全に調べられることが最終価値であるため。
- 決定: 個人の記録体験を知識基盤への入力層と位置づける。Professionalは、契約だけで技量を保証せず、専門外の工場が受託判断と調査を行うためのWebワークスペースとして設計する。
- 影響: 検索結果は根拠、反例、車両一致範囲、安全上の限界を示し、AI独自の故障診断として提供しない。

### 決定: 初期プラン仮説を権限セットとして扱う

- 背景: Free、Owner Plus、Professionalの差を画面や決済サービスへ直接埋め込まず、検証後に変更可能にするため。
- 決定: Freeは車種を問わず所有車両2台と登録車両の検索、Owner Plusは複数台と未登録車種検索、ProfessionalはWebで全車種を対象とする業務検索を初期仮説とする。Freeの広告は将来仮説でありMVPには導入しない。
- 影響: 価格、Owner Plusの台数、OCR・AI枠、広告方式は未確定の設定値として扱う。

### 決定: 貢献評価と情報の正確性を分離する

- 背景: 入力インセンティブが虚偽投稿や過度な承認競争を生まないようにするため。
- 決定: 履歴の継続性、出典追記、結果更新等の行動を可視化できるが、人気や投稿数だけで整備情報の正しさ、整備技量、車両状態を保証しない。Professional契約とメカニック確認も別に扱う。
- 影響: バッジとSNS共有は可能にするが、安全性・検索順位・専門家表示には別の確認基準を用いる。

### 決定: メーター表示値と累積走行距離を分離する

- 背景: 旧車ではメーター故障、複数回交換、修理、リセット、桁あふれ、km/mile変更があり、表示値が正当に減少するため。
- 決定: 車両には複数の`OdometerEpisode`を持たせ、整備時点の表示値を`OdometerReading`として保存する。累積距離は必要な場合だけ根拠と確度を伴う別の推定値とする。
- 影響: 表示値の逆行だけで、虚偽、改ざん、OCR誤り、車両不一致と判定しない。

### 決定: メーター期間は例外処理として段階的に表示する

- 日付: 2026-07-13
- 状態: 暫定
- 背景: 多くのオーナーはメーター交換を経験せず、通常入力やMy Garageで期間管理を前面に出すと負担が大きい。
- 決定: 通常は走行距離だけを入力・表示する。整備記録で「メーターを交換・修理した」を選んだ場合だけ、変更理由と交換後の表示値を入力し、内部で新しい`OdometerEpisode`を作成する。
- 影響: My Garageに期間一覧を常設しない。データモデルの正確性は維持しながら、一般利用者は通常のログアプリとして操作できる。

### 決定: 危険タグから最低警告水準を導出する

- 背景: 投稿者、AI、画面ごとに危険度がばらつき、重大情報の警告が弱まることを防ぐため。
- 決定: タグごとに最低危険度を設定し、複数タグでは最も厳しい条件を採用する。手動で引き上げられるが、自動処理だけで引き下げない。
- 影響: CRITICALは専門家案内、運営確認、表示前警告を要求する。初期分類は公開前に専門家確認する。

### 決定: 症状検索の文章化を公開事例へ拘束する

- 背景: 集合知を文章化する価値を提供しつつ、AI独自の診断やもっともらしい創作を防ぐため。
- 決定: 公開KnowledgeCaseだけを検索・集計し、原因候補、確認箇所、対応、結果を根拠事例IDへ結び付ける。重複資料は独立報告として数えず、反対結果も表示する。
- 影響: AIは検索結果の翻訳・文章化Providerに限定し、根拠外出力は拒否する。

### 決定: 車両画像の公開を安全側のゲートで制御する

- 背景: 自動検出はナンバープレート、顔、住所等を見逃す可能性があり、原本公開は取り返しがつかないため。
- 決定: マスク済み派生画像、メタデータ除去、検出完了、ユーザー目視確認、全候補解決のすべてを公開条件とする。検出ゼロ件だけでは公開を許可しない。
- 影響: 実際の検出Provider導入は実物画像による評価までBLOCKEDとする。

### 決定: ローカル試作データを破棄せずv2へ自動移行する

- 日付: 2026-07-13
- 状態: 暫定
- 背景: 既存プロトタイプは整備記録1件につき作業1件、走行距離1値を前提としていた。
- 決定: localStorage読込時に、旧記録を1作業の整備イベントと初期メーター期間へ変換する。旧フィールドは互換表示用に残し、新規保存の正本は`actions`と`odometerReading`とする。
- 影響: 既存のブラウザ記録を消さずに複数作業・複数メーター期間へ移行できる。本番DB移行方式を確定するものではない。

### 決定: 画像プライバシーUIでは疑似検出を明示する

- 日付: 2026-07-13
- 状態: 暫定
- 背景: 実物画像と精度評価がない段階で自動検出を実装・表示すると、検出能力への誤解を招く。
- 決定: ローカルUIでは固定のDEMO候補領域のみを表示し、実検出ではないことを画面内に明記する。候補マスク後も人による画像全体確認を必須とする。
- 影響: UIと公開ゲートは先に検証できるが、実検出Providerと自動マスキングはMECH-206としてBLOCKEDを維持する。

### 決定: History Levelを信頼評価から分離する

- 日付: 2026-07-13
- 状態: 暫定
- 背景: 工場整備を含むオーナーに記録継続の見返りを返しつつ、件数目的の虚偽や整備能力の誤認を避けるため。
- 決定: 車両ごとの履歴整理状況をHistory Levelと節目で表示する。非公開記録も対象にし、検索順位、情報の信頼度、Professional認証、課金特典には利用しない。
- 影響: 条件と名称は利用テスト後に変更できる純粋なドメイン計算として実装する。

### 決定: 初期SNS共有は安全なテキストプレビューから始める

- 日付: 2026-07-13
- 状態: 暫定
- 背景: 外部共有の楽しさを検証しつつ、ナンバー、費用、走行距離、非公開記録の漏えいと広告SDK等の依存を避けるため。
- 決定: 共有文を事前表示し、ユーザー操作でクリップボードへコピーする。初期文には記録件数と車種名だけを使い、個別記録の内容を含めない。
- 影響: SNS SDK、自動投稿、車両写真付きカードは追加しない。画像共有は公開ゲート実装後に別途判断する。

### 決定: 実資料なしでも抽出結果確認UIを検証する

- 日付: 2026-07-13
- 状態: 暫定
- 背景: 紙の記録簿が用意できるまで、取込確認の操作設計を止めないため。
- 決定: DEMOの`FieldAssertion`を使い、候補値修正、確認、除外、全項目確認後の非公開下書きゲートを実装する。
- 影響: OCR精度、実帳票の項目、原本保持時間、外部AI利用を確定しない。DEMOから実整備記録は保存しない。

### 決定: Garage Journalは本人が自由に書き、AIは代筆しない

- 日付: 2026-07-15
- 状態: 暫定
- 背景: 整備記録の定型情報は履歴管理に必要だが、所有体験の楽しさや本人らしさまでAI生成すると、投稿する意味と信頼性が薄れるため。
- 決定: 日付、走行距離、整備箇所、作業等は関連整備記録から定型表示できる。Journal本文は本人の自由記述とし、AIは本人が許可した本文から整備情報候補を抽出し、検索補助または共有候補確認へ回す用途に限定する。
- 影響: Journal、整備記録、公開ナレッジを別データとして管理する。AI抽出だけで事実確定せず、公開Journalを検索で参照する場合も確認済みKnowledgeCaseと分離表示する。

### 決定: ソーシャル反応とナレッジ信頼度を分離する

- 日付: 2026-07-15
- 状態: 暫定
- 背景: 見せたい・見てもらいたい動機は入力継続に有効だが、人気が正しさを保証せず、虚偽投稿の誘因にもなるため。
- 決定: プロフィール、車両、車種のフォローと時系列フィードを検証する。フォロワー数、閲覧数、リアクション数は、ナレッジ信頼度、検索順位、Professional確認に使用しない。
- 影響: 初期ソーシャル層はGarage Journalとフォローに絞り、DM、グループ、リポスト、ランキング、推薦アルゴリズムは対象外とする。

### 決定: Journalメディア本体を本文データから分離し、公開処理前は非公開に限定する

- 日付: 2026-07-15
- 状態: 暫定
- 背景: 写真・画像・動画は所有体験を伝える重要な要素だが、localStorageへのBase64保存は容量・性能に不向きで、ナンバー、顔、位置、動画音声等の公開事故も防ぐ必要がある。
- 決定: Journalにはメディア参照、種別、説明、公開準備状態だけを持たせ、ローカルプロトタイプのファイル本体はIndexedDBへ保存する。外部送信は行わない。自動マスキング、メタデータ除去、動画確認が未実装の間、ユーザー実ファイルを含む投稿は非公開だけ許可する。
- 影響: 試作スキーマをv4へ移行する。将来の公開では原本と公開用派生ファイルを分離し、画像全体または動画全区間の目視確認を必須とする。

### 決定: Journalはnote型の順序付きブロックで編集する

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 本文末尾への一括添付では、故障や路上停止を含む所有体験を読み物として楽しく構成しにくく、投稿継続の動機を弱めるため。
- 決定: 段落、見出し、引用、写真、動画を順序付きブロックとして任意位置へ挿入できる。AIは本文を生成・書換えしない。Journalを書かず整備記録だけを残す経路も維持する。
- 影響: 試作スキーマをv5へ移行する。メディア本体は引き続きIndexedDBへ分離し、公開ゲートを変更しない。

### 決定: Journalと簡易出来事を「愛車の記録」の入力モードとして扱う

- 日付: 2026-07-21
- 状態: 暫定
- 背景: Journalと出来事を別機能のように表示すると、利用者がどちらへ書くべきか判断できず、過去日付の誤入力を保存後に直せないため。
- 決定: ユーザー向け名称は共通の「愛車の記録」とし、写真と一言の`さっと記録`、タイトル・長文・複数メディアの`詳しく記録`という二つの入力モードを提供する。保存先と車両時間軸は共通とし、本人は保存後も編集できる。
- 影響: 内部の`GarageJournalPost`型と既存データ互換は維持する。編集時は所有者と対象車両所有者をコア処理で再確認し、記録ID、作成日時、既存メディア、反応、モデレーション状態を保持する。

### 決定: 症状検索は根拠付き要約を主表示し、投稿を出典として開く

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 利用者が大量の投稿を一件ずつ読む負担を減らしつつ、AIの断定や創作を検証可能にするため。
- 決定: 公開投稿・事例から確認箇所、原因候補、対応、部品、結果を文章化し、その後に出典リンクを示す。出典外の内容を補完せず、未解決・反対結果も残す。
- 影響: 現行は外部AIを使わないDEMO要約とし、将来Provider接続時も主張と出典IDの対応検証を必須にする。

### 決定: 正式名称をMECHORI（メカリィ）とする

- 日付: 2026-07-16
- 状態: 決定
- 背景: 当初候補MECHORYには同名の既存企業と`.com`利用があり、ブランドの識別性とグローバル展開上の混同を避ける必要があった。
- 決定: 英字表記をMECHORI、日本語読みをメカリィ、タグラインを`Fix. Share. Drive on.`とする。公式ドメインは`mechori.com`とする。
- 影響: 文書、画面表示、npmパッケージ名、共有文、エクスポート名をMECHORIへ変更する。ローカル試作データは旧保存キーから自動移行し、既存Journalメディアの保存領域は互換性のため維持する。正式公開前の類似商標確認は別途行う。

### 決定: 成長判断を登録者数だけで行わない

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 整備は毎日発生せず、登録だけの利用者や投稿件数だけを増やしても、知識基盤と継続収益は育たないため。
- 決定: 記録のある実車両、初回記録、再利用、根拠付き検索、結果追記、車種別ナレッジ密度を段階ごとの合格条件にする。M0の本人検証、M1の10〜20人、M2の100人、M3の初期有料、M4の1,000人で判断する。
- 影響: 集客前に初回価値と計測定義を整える。Owner PlusとProfessionalは具体価格を示した有料意向で検証し、広告は十分な閲覧量とプライバシー審査後に判断する。

### 決定: 室蘭への再投資構想と初期プロダクト判断を分離する

- 日付: 2026-07-16
- 状態: 暫定
- 背景: MECHORIの利益を将来、室蘭へ人と資金を呼ぶ事業へ再投資する長期構想がある一方、法人所在地や税務手段を先に固定すると初期検証を歪める可能性がある。
- 決定: 室蘭への地域還元を所有者側の長期動機として保持する。収益が具体化するまでは、ユーザー価値、継続性、収益性を優先し、法人化、所在地、税務、寄附方法は最新制度と事業実態を基に後で決める。
- 影響: `docs/BUSINESS_GROWTH.md`と`docs/TAX_READINESS_JP.md`で段階を分け、地域構想を初期機能やユーザー向け約束へ混在させない。

### 決定: α版は多車種で検証し、Barchettaを密度アンカーとする

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 最初に協力を依頼できる友人・知人がすでに多車種で、単一車種だけでは共通価値と利用層ごとの違いを判別できないため。
- 決定: αは10車種以上、20〜30台を目安に、旧車・現行車、国産・輸入、DIY・工場依頼等を横断して検証する。FIAT Barchettaは所有者自身が実記録と検索密度を深く確認する密度アンカーとする。
- 影響: 次の外部展開先は先に固定せず、再訪、記録、検索、共有、招待が強い2〜3クラスターをα結果から選ぶ。

### 決定: 公開SNSの基本単位をオーナーと一台の愛車にする

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 複数台所有者の全車両に同じフォロワーが関心を持つとは限らず、愛車との長い関係そのものが投稿・閲覧の動機になるため。
- 決定: `表示名 / 車両`を基本表示とし、プロフィール全体、個別車両、車種を独立してフォローできる。所有開始は年・月だけを最小保持し、車齢、所有年数、節目を愛車プロフィールへ表示できる。
- 影響: 試作スキーマをv6へ移行する。所有歴と節目は、投稿の信頼度、整備能力、検索順位、Professional確認へ利用しない。メーター表示値は累積距離バッジへ直結させない。

### 決定: 予防的インサイトは実例分布を説明し、交換時期を断定しない

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 年式・走行距離・交換履歴から次の点検候補を知る価値がある一方、メーター交換、仕様差、使用環境、母数不足により一律の故障予測は危険なため。
- 決定: 公開実例の分布、車両一致範囲、反対結果、自車の確認済み履歴から「点検候補」「履歴確認候補」「情報不足」を示す。AIはその構造を文章化するだけとする。
- 影響: `docs/PREVENTIVE_INSIGHTS.md`を出力契約とし、現在表示中の走行距離だけで交換命令や虚偽判定を行わない。

### 決定: ローカルの表示安全機能をプロフィール単位で検証する

- 日付: 2026-07-16
- 状態: 暫定
- 背景: フォロー解除だけでは、関係を維持しつつ一時的に投稿を隠すことや、見たくない相手との接点を抑止できないため。
- 決定: ミュートはフォローを維持してフィードだけを非表示にする。ブロックは対象プロフィールと個別車両へのフォローを解除し、投稿、候補、直接Journal閲覧を抑止する。車種フォローは維持する。
- 影響: 試作スキーマをv7へ移行する。状態は本人だけの表示管理へ置き、人気、信頼度、検索順位へ利用しない。本番の双方向制御は認証・RLS設計時に確定する。

### 決定: 招待制ベータはSNS招待URLとGoogleログインを中心に始める

- 日付: 2026-07-16
- 状態: 決定
- 背景: 最初の協力者へSNSで依頼する際、パスワード作成を初回障壁にしない一方、認証Providerを増やしすぎると設定・復旧・障害対応が重くなるため。
- 決定: SNSでは招待URLだけを共有し、初期認証はSupabase AuthのGoogleを中心とする。Apple、X、メールのマジックリンクは後段候補、Facebookは初期対象外とする。通知はアプリ内を先に作り、Web Pushを後段、メールを重要連絡と復旧へ限定する。
- 影響: 友人一覧やSNS投稿を取得せず、メール招待と日常メール通知へ依存しない。内部ユーザーIDとの分離、Google Provider喪失時の復旧、招待失効、RLSを接続前に確認する。外部接続はまだ行わない。

### 決定: 参加者自身が1人用招待を発行できる

- 日付: 2026-07-22
- 状態: 決定
- 背景: 運営者が外部管理画面を開いて毎回URLを発行すると、友人紹介の自然なタイミングを逃し、運用負荷も高くなるため。
- 決定: 有効な参加権限を持つユーザーはMECHORI内から、7日間・1アカウント限りの招待を発行できる。共有、URLコピー、ブラウザ内生成QRを提供し、未使用3件・直近30日10件を上限とする。
- 影響: 招待者を`created_by_user_id`で追跡し、共通URLや無制限招待にはしない。Google OAuthがテストモードの間は、テストユーザー登録だけ運営作業として残る。

### 決定: 本番認証の前にProvider非依存のローカルモックで公開境界を検証する

- 日付: 2026-07-16
- 状態: 暫定
- 背景: Provider選定やSupabase接続を待たずに、未ログイン閲覧、招待登録、保護画面、ログアウトの体験とプライバシー境界を固めるため。
- 決定: 認証状態をアプリケーションデータから分離し、Google、Apple、Facebook、X、メールの選択を端末内DEMOセッションへ変換する。認証状態が未保存・不正な場合は未ログインとし、公開ホーム、公開ナレッジ、公開Journalだけを表示する。
- 影響: 外部通信やメール保存なしで画面フローを確認できる。本物の本人確認、OAuth、メール配信、Provider統合、復旧、サーバーセッション、RLSは提供せず、MECH-101の所有者判断を置き換えない。

### 決定: 長い入力を正式データと分離した端末内下書きで保護する

- 日付: 2026-07-16
- 状態: 暫定
- 背景: 整備記録とJournalは入力項目が多く、再読み込みや保存容量不足で途中内容を失うと初期利用の継続性を損なうため。
- 決定: バージョン付き下書きを正式な`AppData`と別キーへ保存し、復元・破棄を可能にする。空フォームは保持せず、Journalのローカルメディア本体と一時URLは下書きへ保存しない。
- 影響: 本保存はDataProviderへの書込み成功後に画面へ反映する。保存失敗時は下書きを残して明示する。本番ではユーザー単位の暗号化・保存期間・端末共有リスクを認証とDB設計時に再確認する。

### 決定: 公開設定と安全操作を対象ごとに独立させる

- 日付: 2026-07-16
- 状態: 暫定
- 背景: プロフィールを公開したことが非公開整備記録や写真の公開を意味したり、通報件数が投稿者の信頼度へ転用されたりする事故を避けるため。
- 決定: プロフィール、Journal、整備記録共有候補、写真、ナレッジ抽出同意を別設定にする。通報は個人情報等の理由別受付とし、人気・信頼度・Professional確認へ使用しない。一時非公開は削除と分ける。
- 影響: 試作スキーマをv8へ移行する。ローカルでは状態遷移を検証し、本番の運営者権限、RLS、通知、監査、異議申立てはMECH-106で確定する。

### 決定: 継続指標はMAUとValue MAUを対で扱う

- 日付: 2026-07-17
- 状態: 暫定
- 背景: 整備は低頻度だが、半年間忘れられる状態では次の整備結果やJournalが蓄積されない。一方、DAU、連続ログイン、空の再訪を最適化すると製品の軸がSNS滞在時間へずれるため。
- 決定: 暦月内にログインした重複なし利用者をMAU、愛車、履歴、検索、フォロー更新、記録・Journalのいずれかへ到達した利用者をValue MAUとする。Value MAU Rateと3か月連続Value Returnを主要な継続指標にし、DAUは診断値に限定する。
- 影響: ホームへ月次の愛車状況を追加する。外部分析SDKは導入せず、試作は個人情報を含まない端末内イベントだけで契約を検証する。

### 決定: 車種マスタにない愛車も即時登録できる

- 日付: 2026-07-17
- 状態: 決定
- 背景: 初期参加者は新旧・国内外の趣味車にまたがり、希少車、並行輸入、限定仕様、別名表記が多い。固定マスタを登録ゲートにすると、最も必要なオーナーを排除するため。
- 決定: メーカー・車種のユーザー入力を正本として愛車を即時作成する。年式、型式、エンジン等は不明を許容し、後から補完する。将来の車種マスタは別名統合と検索精度のために参照し、登録承認や利用制限には使わない。
- 影響: ローカル試作へ自由入力フォーム、複数台切替、任意年式を追加する。本番ではユーザー入力値と正規化参照IDを分離する。

### 決定: メーカー表記と市場別車名を別レイヤーで正規化する

- 日付: 2026-07-21
- 状態: 決定
- 背景: `FIAT` / `フィアット`等の言語差と、`ヴィッツ` / `YARIS`等の市場別名称を文字列比較すると、同じ車系統の記録・フォローが分裂する。一方、車名だけで全世代・市場仕様を同一とみなすと誤統合になる。
- 決定: メーカーは既知別名をアルファベット表記へ統一し、入力原文を別保持する。車名は`modelFamilyId`、`generationId`、`marketNameId`へ分離し、登録を止めない。写真認識は将来の未確認候補とし、本人確認なしに正規IDへ昇格しない。
- 影響: αの小規模辞書で別名照合と車系統フォローを検証する。全世界カタログ、世代判定、外部画像認識は別タスクとする。

### 決定: Web MVPの外部基盤をSupabase・Netlify・OpenAI API前提で設計する

- 日付: 2026-07-17
- 状態: 決定
- 背景: 個人開発中の固定費を抑えながら、招待制ベータへ認証・永続化・画像保存・Web配信・AI補助を段階導入する必要があるため。
- 決定: SupabaseをDB・認証・Storage、NetlifyをWeb配信、OpenAI APIを限定的なAI補助の採用前提とする。Provider固有処理はAdapterへ隔離し、採用方針と実接続・課金・本番利用を別ゲートにする。
- 影響: Vercel候補表記をNetlifyへ更新する。AIはキャッシュ、最大原価見積り、月次全体、利用者別日次原価、利用者別日次回数を通過した場合だけ呼び、上限時は非AI機能へ縮退する。アカウント作成、秘密情報、RLS、本番接続、費用上限値は別途承認する。

### 決定: Supabaseを継続し、ボトルネック単位で可搬性を保つ

- 日付: 2026-07-29
- 状態: 決定
- 背景: P0ではSupabaseによるDB、認証、Storageの一体運用が開発・運用負担を抑える一方、規模拡大後の費用と運用負荷は機能ごとに異なるため。
- 決定: 現時点ではSupabaseを継続利用する。永続データは標準PostgreSQLへ移行可能な構造を維持し、認証、RLS、Storage等のProvider固有処理をドメインモデルへ不要に密結合しない。規模拡大時はDB、認証、Storage、通信、運用人件費を個別に比較し、全面移行ではなく、確認されたボトルネック単位の分離を優先する。
- 影響: P0のために過剰な抽象化や先行移行は行わない。Supabase固有機能の採用理由と移行時の変換点を追跡し、αの一時Workspaceを将来の正規化データ構造として固定しない。

### 決定: 初期UIは日英、データと構造は多言語追加を前提とする

- 日付: 2026-07-17
- 状態: 決定
- 背景: 将来EU圏を含む自動車文化の強い地域へ展開する際、原文、検索コード、DB、画面を日英二択から作り直さないため。
- 決定: UI対応言語は追加可能な一覧とし、初期提供を日本語・英語に限定する。投稿原文言語はBCP 47タグで任意言語を保持し、翻訳、地域、タイムゾーン、単位、通貨と分離する。
- 影響: 言語選択を二択トグルから選択リストへ変更する。翻訳は原文版に紐づく派生データとし、原文を上書きせず、独立事例として二重計上しない。追加言語の公開は安全・法務・主要導線の翻訳確認をゲートとする。

### 決定: 遠隔αはGoogle認証と利用者別Workspaceを接続する

- 日付: 2026-07-17
- 状態: 決定
- 背景: 3〜5人が各自の端末で愛車と整備履歴を試すには、端末内認証モックとlocalStorageだけでは本人確認、再ログイン、利用者分離、データ消失を検証できないため。
- 決定: ローカルDEMOを維持し、`alpha`実行時だけSupabase Google OAuth、1人用招待、active参加権限、利用者別`alpha_private_workspaces`へ切り替える。新規利用者はデモ車両ではなく空の非公開Garageから開始する。
- 影響: 招待の生トークンはアクセスログへ送られないURLフラグメントで受け取り、OAuth往復中だけ短命HttpOnly Cookieへ移す。OAuth先へは渡さない。α WorkspaceのJSON保存は3〜5人検証用の暫定境界であり、β前に車両・整備イベントを正規化する。実車検証に必要な写真は、元画像を保持せずブラウザで縮小・再生成した小容量WebPだけを暫定的にWorkspaceへ保存し、β前に非公開Storageへ移行する。

### 決定: αのMAU計測は月次の最小行へ集約する

- 日付: 2026-07-17
- 状態: 暫定
- 背景: DAUを追わずMAUとValue MAUを評価する一方、外部分析SDKや詳細な閲覧ログは少人数αに不要なため。
- 決定: 1利用者・1 UTC月につき1行だけ保持し、初回・最終セッション日時、Value行動の有無、許可済みValueイベント名の集合を記録する。検索語、ページ、車種、整備内容、Journal本文、滞在時間は記録しない。
- 影響: DBマイグレーションと環境設定の両方が完了したときだけ送信する。オーナーは集計値だけを`/settings/alpha`で確認し、利用者ランキングを作らない。

### 決定: GTM経由でGA4とClarityを補助計測に使う

- 日付: 2026-07-18
- 状態: 決定
- 背景: αテストで、ページ導線、操作上の詰まり、表示・クリック傾向を遠隔から確認する必要があるため。
- 決定: `GTM-M54GKLLL`を全ページの共通レイアウトへ設置し、GA4とMicrosoft ClarityだけをGTM経由で導入する。MAU・Value MAUは引き続き内部の月次最小計測を正本とする。
- 影響: Clarityではアプリ全体を明示マスクし、利用者が入力・保存した内容を画面記録へ送らない。GA4・Clarityへ利用者IDや車両・整備・投稿内容をカスタム送信しない。GTMコンテナのタグ・変数・送信先変更は外部データ送信範囲の変更として事前確認する。EU向け提供前に同意管理を実装する。

### 決定: 過去所有車両を削除せず同じ車両履歴として扱う

- 日付: 2026-07-18
- 状態: 決定
- 背景: 過去に所有したクルマ・バイクには、現在所有者だけでは得られない故障、修理、部品、費用、使用環境の経験があり、本人のカーライフとしても将来のナレッジとしても失わない必要があるため。
- 決定: クルマとバイクを共通Vehicleとして扱い、現在所有、過去所有、未設定を所有関係で区別する。所有終了時はVehicleを削除・複製せず、同じIDのまま「これまでの愛車」へ移す。過去車は車両種別、メーカー、車名だけで直接登録でき、写真、年式、所有時期を必須にしない。
- 影響: 試作スキーマをv9へ移行する。整備記録へ作業当時、資料、写真・整備記録簿、後日の記憶、不明の由来を追加し、検索・AI整理で同じ確度として扱わない。初回の現在車登録は写真中心のまま維持し、過去車登録はGarageから任意で始める。

### 決定: 検索流入と第三者AI利用を分離する

- 日付: 2026-07-18
- 状態: 決定
- 背景: 公開Journal・整備事例・構造化ナレッジが検索から必要なオーナーへ届く価値は必要だが、公開を第三者AIの学習、外部RAG、データセット化への包括許諾にすると、投稿者の体験とMECHORIの集合知が無断で代替利用されるため。
- 決定: α環境はすべての自動クローラーから除外する。正式公開後は通常検索の索引を許可し、AI学習、TDM、外部ナレッジ化、大量取得、再配布を拒否する。AI回答・AI検索専用クローラーは個別評価まで拒否する。robots.txt、noindex、TDM権利留保、利用規約、公開最小化、監視を組み合わせ、いずれも完全な技術的強制ではないことを明示する。
- 影響: `docs/AI_CRAWLING_POLICY.md`を正とし、`/ai-policy`、環境別robots、αの`X-Robots-Tag`、`TDM-Reservation`、`/.well-known/tdmrep.json`を追加する。Google検索内AI機能だけをSEOと分離できない制約はSearch Consoleの実測後に再評価する。
### 決定: 特に親しい3〜5人の遠隔αを、SNS知人向けβの前に行う

- 日付: 2026-07-17
- 状態: 決定
- 背景: 所有者が横で観察できる協力者を確保しにくい一方、ローカルDEMOから10〜20人の招待制βへ直接進むと、権限・データ消失・日本語・初回理解の問題を多人数へ広げるため。
- 決定: 最初に特に親しい友人3〜5人へ、1人1つの期限付き招待URLで遠隔αを行う。重大問題を修正した後、SNSでつながる知人10〜20人へ招待制βを広げる。αはGoogleログイン、Supabaseのデータ分離とRLS、Netlifyの検証URLを使い、OpenAI API、OCR、決済、日常メール通知は使わない。
- 影響: `docs/ALPHA_PLAYBOOK.md`をαの正とする。一般公開、共通招待コード、URL転送による参加拡大は行わない。α用外部接続とデプロイは所有者の個別承認後に実施する。

### 決定: αはMinimum Lovable Productの縦体験を最優先にする

- 日付: 2026-07-17
- 状態: 決定
- 背景: 認証、整備入力、検索、SNSを横並びで実装しても、初回利用者が自分のクルマを登録したくなる感情と、一人で使う価値が成立しないため。
- 決定: 招待・Googleログイン、写真中心の約3分愛車登録、登録完了、写真と一言の出来事、車齢・所有年月を含む愛車時間軸、明示的な共有ページ作成を一つの合格フローとする。初回は型式、詳細仕様、走行距離、愛車の呼び名を要求しない。
- 影響: 車両未登録ホームは検索や空フィードではなく愛車登録へ誘導する。共有ページは非公開Workspaceの閲覧権限を緩めず、最小項目の公開用写しを別テーブルへ作成し、公開停止できるようにする。AIはこのフローを遅らせず、外部APIを呼ばない。

### 決定: 愛車の出来事は発生日の精度を保持する

- 日付: 2026-07-21
- 状態: 決定
- 背景: 現在車・過去車を問わず、過去の修理、ドライブ、トラブルは正確な日を覚えていないことがあり、便宜的な1日を保存すると後の履歴・検索・AI整理で誤った事実になるため。
- 決定: Garageの記録は日付、年月ごろ、年ごろ、時期不明を区別して保存する。表示と並び順にも精度を引き継ぎ、入力されていない月日を推測しない。本人は保存後も内容と発生時期を再編集できる。
- 影響: Journalと簡易出来事へ共通の発生時期入力を追加する。既存の正確な日付はそのまま扱い、構造化整備記録への同モデル適用はMECH-040で行う。

### 決定: αの基調色を特定国・車種に寄らないニュートラル基盤へ移す

- 日付: 2026-07-21
- 状態: 暫定
- 背景: 深い緑を広い面積で使うと英国車サービスの印象が強く、多国籍・多年代の趣味車を扱うMECHORIの対象を狭く見せるため。
- 決定: 面と本文はニュートラルな白・灰・チャコールを基盤とし、主要操作は落ち着いたサービスブルー、節目はアンバー、成功・安全状態だけに緑を使う。色だけで状態を伝えない。
- 影響: αでブランド印象と可読性を確認する。ロゴ、最終ブランド色、Professional画面の配色はMECH-103で確定する。

### 決定: Professionalは知識の権利管理と工場DXを同じ案件基盤で段階検証する

- 日付: 2026-07-21
- 状態: 暫定
- 背景: 専門店のノウハウを無償開示させると参加が続かない一方、工場内だけに閉じると、専門知識のない地域工場でも趣味車を維持できる未来が遠のく。帳票の再入力削減は、工場が記録を続ける別の強い利益になり得るため。
- 決定: 工場内限定、対応経験シグナル、許諾ナレッジを主張単位で分ける。公開知識で足りない案件は工場間の有償相談へつなぎ、採用された許諾知識は利用追跡とクレジット・将来分配の対象候補にする。同じ案件データから作業報告、見積、発注、請求等を作る工場DXは、一つの反復業務から検証する。
- 影響: 非公開原文を無断でモデル学習・共有回答へ転用しない。国別帳票は共通データと人間確認済み地域パックを分離し、AIは制度調査とテスト補助に限定する。α・初期MVPにはProfessional機能を追加しない。

### 決定: メーカーをまたぐ関連車両を単一車種へ潰さない

- 日付: 2026-07-21
- 状態: 決定
- 背景: OEM姉妹車、市場別ブランド、ブランド移管、正規な設計継承、Seven型の派生車は、外観や基礎設計が近くても部品・型式・整備上の互換性が同じではないため。
- 決定: ブランド、市場名、車系統、世代を保持し、車両間を`market_name_variant`、`oem_rebadge`、`brand_transition`、`licensed_continuation`、`inspired_derivative`で結ぶ。後二者は別の車系統IDを維持する。
- 影響: 関連事例は発見できるが、検索・AI整理では関係種別に応じて完全一致と分離する。写真や車名だけで世代、仕様、部品適合を確定しない。

### 決定: 同一車名・同一世代でも仕様系統を完全一致から分離する

- 日付: 2026-07-21
- 状態: 決定
- 背景: R33スカイラインのGTS25tとGT-Rのように、同じ車名・世代でも型式、エンジン、駆動系、部品が大きく異なる車両があり、車系統や世代だけの一致では整備ナレッジを混同するため。
- 決定: `modelFamilyId`、`generationId`の下に`variantId`を置き、ユーザー入力の`grade`と`modelCode`は別に保持する。型式一致のみ確認済みとし、グレード候補、仕様不明、入力矛盾を区別する。
- 影響: 現在車・過去車とも任意のグレード・型式を登録後に編集できる。検索・AI整理は完全一致、同世代別仕様、同車系統別世代、仕様不明を分離し、仕様不明を完全一致件数へ含めない。

### 決定: 派生系統と具体仕様、個体の機械仕様を分離する

- 日付: 2026-07-21
- 状態: 採用
- 背景: PEUGEOT 205 GTI 1.6 / 1.9、205 Turbo 16、LANCIA Delta HF各進化型のように、グレード名の一階層だけでは整備適用範囲を表現できない。同一グレード名でも市場・年次・機関・変速機が異なる場合がある。
- 決定: `variantId`を派生系統、`configurationId`を具体仕様として分離する。エンジン型式、排気量、過給方式、駆動方式、ミッション型式、仕様補足を車両個体へ任意保存する。カタログ未登録時も、申告機械仕様の一致・相違を検索確度へ反映する。
- 影響: 初回登録はメーカー・車名のみで完了できる。詳しい所有者は折りたたみ項目または登録後編集で仕様を追加でき、未確認入力は完全一致へ昇格しない。

### 決定: 愛車管理・SNSとの重複を認め、整備知識循環を差別化の核にする

- 日付: 2026-07-24
- 状態: 決定
- 背景: MyGarage等の既存サービスにも、現在・過去の愛車、スペック、複数台管理、写真、イベント等があり、愛車プロフィールや使いやすさだけでは後発の追加利用理由にならないため。
- 決定: 入口は愛車履歴として気持ちよく使える状態を維持し、整備・故障・部品・対応結果を同じ車両へ接続し、同型車の次の整備へ還元する循環を中核にする。イベント、グループ、カレンダー、人気競争は初期の競争領域にしない。
- 影響: 未ログイントップは、愛車管理だけでなく「履歴が次の整備へ使える」と説明する。未提供のAI・実在ナレッジを現在形で約束せず、αでは最初の説明なしに履歴・整備・同型車経験の関係が伝わるかを確認する。詳細は`docs/COMPETITIVE_POSITIONING.md`を正とする。

### 決定: 「整備士のGitHub」をProfessionalの中核設計にする

- 日付: 2026-07-25
- 状態: 決定
- 背景: 整備知識を共有してもらうには、送客や対価だけでなく、整備士本人が積み上げた確認・訂正・結果追跡を技術実績として示せる利益が必要である。ベテランの知識継承と、若手・地域工場の調査支援も同じ履歴基盤で扱える。WebエンジニアがGitHub上の成果を職務経歴の補助に使うように、整備士にも所属先を越えて持ち運べる技術履歴が必要である。
- 決定: Professionalを、事例、未解決事項、追加・訂正提案、レビュー、結果履歴が育つ「整備士のGitHub」として設計する。許諾済みの採用知見、対応経験、レビュー、訂正、結果追跡を、本人が管理できる技術ポートフォリオへ接続し、就職、転職、独立、協業時に使える技術経歴書として共有できるようにする。
- 影響: GitHubの画面・用語を模倣せず、一般向けの訴求とも分ける。投稿量、人気、契約だけで技術力や安全性を保証せず、資格・本人性・所属・経験根拠を分離する。一般公開、採用先限定URL、PDF等の共有形式を検証する。顧客情報、勤務先の企業秘密、非公開案件を持ち出さず、個人と工場の実績帰属、非公開化、在籍確認、退職・転職時の扱いをProfessional実証前に定める。世界的な専門性は単一ランキングで認定せず、車種・仕様・分野・期間・根拠を限定した貢献履歴から発見できるようにする。

### 決定: Professional品質の共通コアと目的別サーフェスを採用する

- 日付: 2026-07-25
- 状態: 決定
- 背景: OwnerとProfessionalは同じ車両、整備、症状、部品、結果を扱うが、Ownerは愛車との時間を軽く楽しく残すこと、Professionalは案件、根拠、権利、レビュー、技術実績を扱うことが主目的である。Professional画面から機能を隠しただけのOwner版では、業務フォーム化して初回体験を損なう。
- 決定: 車両、出来事、整備、症状、部品、出典、確認、訂正、公開範囲、多言語をProfessional品質の共通ドメインコアとする。その上にOwner、Professional、Public Knowledgeの目的別ユースケースと画面を構成する。Ownerは機能簡略・楽しさ重視の専用体験とし、Professionalの劣化版とは位置づけない。
- 影響: Ownerの称号・反応・DIY記録をProfessionalの技能確認へ昇格させない。顧客情報、工場内案件、公開ナレッジを分離する。オーナーから工場への履歴共有、工場からオーナーへの結果返却、共有ナレッジ候補化は、対象項目、同意、権限、版を持つ明示的なユースケースとして設計する。プラン権限と画面・ナビゲーションの構成を分離する。

### 決定: Professionalは熟練者に耐える症例エンジンを実績表示より先に作る

- 日付: 2026-07-25
- 状態: 決定
- 背景: 著名人向けの送客、ランキング、プロフィールだけでは、整備士として製品を高く評価する理由にならない。実案件で仕様差、判断経路、反対結果、測定、部品関係を追えることが、知識共有と技術ポートフォリオの前提になる。
- 決定: Professionalでは、項目別の仕様一致・相違・不明、ケースリプレイ、変化なし・再発・適合失敗、条件付き測定値、部品・機構関係、根拠付き項目レビュー、工場内案件再利用を中核体験とする。AIは未確認候補の整理と根拠付き文章化に限定し、診断、正常範囲、適合、未実施作業を補完しない。
- 影響: 技術ポートフォリオは中核体験への採用、確認、訂正、結果追跡から育てる。MECH-043で実案件の手作業プロトタイプを先に検証し、ランキング、総合スコア、著名人向け機能は後段にする。α・初期Owner体験へ業務入力を持ち込まない。

### 決定: P0・Closed Alphaでは明示確認済みのJournal写真を参加者限定で共有する

- 日付: 2026-07-29
- 状態: 暫定
- 背景: 文字情報だけのフィードでは、愛車を見せる楽しさと参加者同士の関係が弱く、P0で「愛車の履歴を共有したくなるか」を十分に検証できない。一方、一般公開用の自動マスキングは未実装であり、写真共有を本文公開へ暗黙に含めることはできない。
- 決定: P0・Closed Alphaに限り、本人がJournalの参加者向け共有とは別に添付写真の共有を確認した場合だけ、ブラウザ内で再生成した512KB以下のJPEG、PNG、WebPを非公開Storageへ保存する。参加状態が有効な利用者だけが、可視状態の共有Journalから参照されている画像を取得できる。動画、原本、EXIF、非公開指定画像は共有しない。
- 影響: 新規記録は非公開、写真共有は未選択を初期値とする。共有停止後は他の参加者から画像を取得できないよう、共有行をStorage RLSの閲覧ゲートにする。これは招待制αの限定検証であり、一般公開、公開ナレッジ、β以降の画像公開条件を緩和しない。ナンバープレート、人物、住所を推測できる背景等は自動で消えないため、共有前の目視確認を画面で求める。

### 決定: 共有Journalの写真は本文と同じMECHORI内公開範囲へ統一する

- 背景: 写真だけの公開切り替えは保存・共有版管理を複雑にし、本文を共有したのに写真が見えない体験を生んだ。一方、現段階で未ログインのWeb全体へ写真を公開する方針ではない。
- 決定: 写真単独の公開設定を廃止し、共有Journalの再生成済み写真は本文と同じく、有効な参加状態を持つログイン済みMECHORI利用者へ表示する。Storage bucketは非公開のままとし、未ログイン閲覧、原本、下書き・非公開記録、動画は対象外とする。
- 影響: 既存写真レコードやStorage objectを一括変更しない。現在の共有用コピーは有効なMECHORI利用者が取得できるようStorage SELECT policyを整合させる。将来の未ログインWeb公開や有料プランの非公開機能は、公開条件、移行、既存データ保護を別途決定する。

### 決定: 直近のプロダクト判断を既存文書へ正式反映する

- 日付: 2026-08-07
- 状態: 決定
- 決定: P-075は人と愛車のつながりを管理・探索するA/B/C段階で設計する。βまでにFounding Garageを少なくとも1軒、個人アカウント・組織・多対多membership・工場内roleで共同開発する。MECHORI運営の`platform_super_admin`は工場membershipと分離する。基本体験は無料とし、AI/OCRの通常利用、記録、閲覧、共有等を意図的に課金壁で不便にしない。原価上限と利用枠を計測してから追加枠・決済を判断する。ネイティブアプリは正式公開後ではなくβ中盤に着手する。P-074のPCは常設左サイドバーへ統合し、PCのハンバーガー、drawer、FABを廃止する。フィードバックは種別選択、管理用status、絞り込み、一括出力を備える方向とする。ビジュアルは`Global Product / Hokkaido Born`を恒常方針とする。
- 正本: 詳細は`docs/SOCIAL_LAYER.md`、`docs/PROFESSIONAL_PLATFORM.md`、`docs/MONETIZATION.md`、`docs/MEASUREMENT_PLAN.md`、`docs/ROADMAP.md`、`docs/DESIGN_DIRECTION.md`、`docs/ux/`の各文書に置く。
- 影響: これは採用済みの設計判断であり、直ちにコード、DB、課金、外部サービスを変更するものではない。各項目の実装・検証・人間QAは、現在の状態文書とロードマップで別々に管理する。

### 決定: αの初回Activationは短い価値説明と行動導線を優先する

- 日付: 2026-08-11
- 状態: 決定
- 背景: 招待URLを開いたαユーザーが、MECHORIの目的と最初の行動を説明なしでは理解できなかったため。
- 決定: 未ログインHomeは「愛車との時間を、記録して、つないで、残していく。」を主メッセージとし、CTAは「MECHORIをはじめる」を一つ明確にする。認証後は、愛車の履歴、人とクルマのつながり、経験の還元を伝える3ステップ案内と、愛車登録・最初の記録・知り合い探しの実データ連動チェックリストを提供する。αではtext/icon中心とし、完了状態はprofile ID単位のlocalStorageへ保持する。
- 影響: 招待、認証、RLS、DBスキーマは変更しない。既存ユーザーはスキップ可能で、既存の愛車・記録・フォロー状態を再利用する。スクリーンショット、動画、motionによるOnboarding強化はβで検討する。

## 未確定

- 認証方式
- 管理者運用
- 料金モデル
- 本番実装時のリポジトリ構成の最終確定
- FIAT Barchettaの仕様分類粒度
- Owner Plusの登録台数と各プランの価格・利用枠
- 貢献バッジの名称、基準、表示範囲
- Professionalの工場間相談料金、知識還元方式、最初の帳票、最初の対象地域
